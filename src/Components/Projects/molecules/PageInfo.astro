---
const { title, text, linkPage, linkGit } = Astro.props;
---

<div class="w-full flex flex-col gap-5">
  <!-- Título -->
  <div class="w-full my-2.5 h-[50px] flex justify-center">
    <h1 class="text-primary text-[50px] font-bold">
      {title}
    </h1>
  </div>

  <!-- Texto -->
  <div class="w-full my-2.5 h-[100px]">
    <p class="text-[aliceblue] text-center">
      {text}
    </p>
  </div>

  <!-- Botones -->
  <div class="w-full h-[50px] flex justify-around">
    <button
      class="w-[48%] h-[30px] py-4 text-primary bg-hard-purple border-2 border-primary rounded-md transition duration-500 hover:bg-primary hover:text-[antiquewhite] cursor-pointer flex justify-center items-center",
      onclick={`window.open('${linkPage}', '_blank')`}
    >
      Page
    </button>

    <button
      class="w-[48%] h-[30px] py-4 text-primary bg-hard-purple border-2 border-primary rounded-md transition duration-500 hover:bg-primary hover:text-[antiquewhite] cursor-pointer flex justify-center items-center"
      onclick={`window.open('${linkGit}', '_blank')`}
    >
      Github
    </button>
  </div>
</div>
